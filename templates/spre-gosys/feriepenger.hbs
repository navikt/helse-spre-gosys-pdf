<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>Utbetaling av feriepenger i ny løsning</title>
    <style>
        @page {
            size: A4 portrait;
            @bottom-left {
                content: element(footer);
            }
        }

        * {
            font-family: "Source Sans Pro";
        }

        h1 {
            margin: 0;
            font-weight: bold;
            font-style: normal;
            font-size: 16pt;
        }

        h2 {
            margin: 0 0 0.2cm;
            font-size: 14pt;
            font-weight: bold;
        }

        h3 {
            margin: 0;
            font-size: 14pt;
            font-weight: bold;
        }

        h4 {
            margin-bottom: 0;
        }

        p {
            font-size: 14pt;
            margin: 0;
        }

        td {
            padding: 0;
        }

        td p {
            margin: 0.1cm 0;
        }

        .header {
            display: block;
            padding: 5mm;
            background: #E0F5FB;
        }

        .header__tittel {
            display: inline-block;
            position: absolute;
            top: 1.2cm;
            left: 3cm;
        }

        .header__navlogo {
            display: inline-block;
            width: 2cm;
        }

        #footer {
            position: running(footer);
            font-size: 12pt;
            margin-top: -13.3mm;
            padding-left: 0.6cm;
            border-bottom: 2px solid black;
            height: 0.6cm;
        }

        #pagenumber:before {
            content: counter(page);
        }

        #pagecount:before {
            content: counter(pages)
        }

        .sidetall {
            float: right;
        }

        .strek {
            border: none;
            border-top: 1px solid black;
            margin-bottom: 11mm;
            margin-top: 0;
        }

        .personinfo {
            display: inline-block;
            width: 100%;
            margin-top: 0.7cm;
            margin-bottom: 4mm;
        }

        .personinfo__header {
            margin-bottom: 7mm;
        }

        .personinfo__personalia {
            display: inline-block;
        }

        .personinfo__ikon {
            display: inline-block;
            height: 1cm;
            margin-right: 0.5cm;
            margin-left: 0.4cm;
            margin-top: 0;
        }

        .personinfo__arbeidsgiver {
            display: inline-block;
            float: right;
        }

        .personinfo__verdi {
            margin-bottom: 4mm;
        }
    </style>
</head>
<body>
<div id="footer">
    <span class="sidetall">Side <span id="pagenumber"></span> av <span id="pagecount"></span></span>
</div>
<div class="header">
    <img class="header__navlogo" src="{{ image "Navlogo.png" }}"/>
    <h1 class="header__tittel">Utbetaling av feriepenger i ny løsning</h1>
</div>
<div class="personinfo">
    <div class="personinfo__verdi">
        <h3>Navn</h3>
        <p>{{ navn }} ({{ insert_at fødselsnummer 6 }})</p>
    </div>
    <div class="personinfo__verdi">
        <h3>Arbeidsgiver</h3>
        <p>{{ organisasjonsnavn }} ({{ orgnummer }})</p>
    </div>
    {{#each oppdrag}}
        <div class="personinfo__verdi">
            <h3>
                {{#eq type "ARBEIDSGIVER"}}
                    Utbetaling til arbeidsgiver
                {{/eq}}
                {{#eq type "PERSON"}}
                    Personutbetaling
                {{/eq}}
            </h3>
            <p>{{ organisasjonsnummer }}</p>
        </div>
        <div class="personinfo__verdi">
            <h4>Periode</h4>
            <p>{{ iso_to_date fom }} - {{ iso_to_date tom }}</p>
        </div>
        <div class="personinfo__verdi">
            <h4>Fagsystem ID: </h4>
            <p>{{ fagsystemId }} </p>
        </div>
        <div class="personinfo__verdi">
            <h4>
                Mottaker
                {{#eq type "ARBEIDSGIVER"}}
                    (orgnummer)
                {{/eq}}
                {{#eq type "PERSON"}}
                    (personnummer)
                {{/eq}}
            </h4>
            <p> {{ mottaker }}</p>
        </div>
        <div class="personinfo__verdi">
            <h4>Totalbeløp</h4>
            <p> {{ currency_no totalbeløp }} kr</p>
        </div>
        {{#unless @last}}
            <hr class="strek"/>
        {{/unless}}
    {{/each}}
</div>
</body>
</html>
